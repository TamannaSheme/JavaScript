var signal = 'green';
console.log(signal);

if (signal == 'red') {
    console.log("Crossing Road might be danger");
} else if (signal == 'yellow') {
    console.log("You should stop and wait for the green the signal.");
} else if (signal == 'green') {
    console.log("You can cross the road");
}